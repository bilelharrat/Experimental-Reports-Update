import SwiftUI

/// Kept for ReportNav + shared search helpers used from Home.
/// Search UI lives on Home — there is no separate Research tab.

struct ReportNav: Hashable, Identifiable {
    let id: String
}
